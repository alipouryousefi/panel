"use client";

import { Box } from "@mui/material";
import ReactLoading from "react-loading";

const Loading = () => {
  return <ReactLoading type="spin" color="#357EDD" />;
};

export default Loading;
